{
    "name": "turing-complete-clicker",
    "version": "1.4.3",
    "description": "Solves levels like e.g the binary racer level",
    "main": "build/index.js",
    "private": true,
    "scripts": {
        "compile": "tsc",
        "start": "npm run compile && node .",
        "dev": "nodemon --watch src/ -e .ts --exec \"npm start || exit 1\""
    },
    "author": "Totto",
    "license": "MIT",
    "dependencies": {
        "screenshot-desktop": "^0.0.0-development"
    },
    "devDependencies": {
        "@types/node": "^18.14.0",
        "@types/screenshot-desktop": "^1.12.0",
        "typescript": "^4.9.5"
    },
    "engines": {
        "node": ">=18.2.0",
        "npm": ">=8.10.0"
    }
}
